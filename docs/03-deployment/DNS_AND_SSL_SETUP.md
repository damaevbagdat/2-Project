# 🌐 Настройка DNS и SSL сертификата

## 📋 Текущий статус

### DNS Записи
- ✅ **A запись настроена**: `alash-zan.kz` → `194.32.142.237`
- ✅ **AAAA запись настроена**: `alash-zan.kz` → `2a00:5da0:1000:1::285a`
- ⏳ **Статус**: Ожидание распространения DNS (обычно 1-24 часа)

### Текущая конфигурация
- ✅ Сайт временно доступен по IP: http://194.32.142.237/
- ❌ Nginx пока настроен на IP адрес
- ❌ SSL сертификат еще не установлен

---

## 🔍 Проверка DNS записей

### Метод 1: С локального компьютера (Windows)
```powershell
# Проверка A записи
nslookup alash-zan.kz

# Проверка с конкретным DNS сервером (Google DNS)
nslookup alash-zan.kz 8.8.8.8
```

**Ожидаемый результат:**
```
Server:  dns.google
Address:  8.8.8.8

Name:    alash-zan.kz
Address:  194.32.142.237
```

### Метод 2: С сервера через SSH
```bash
ssh -i docs/03-deployment/Claude-Alash-Zan-key.pem ubuntu@194.32.142.237

# На сервере выполнить:
dig alash-zan.kz +short
dig www.alash-zan.kz +short
```

**Ожидаемый результат:**
```
194.32.142.237
```

### Метод 3: Онлайн сервисы
- https://www.whatsmydns.net/#A/alash-zan.kz
- https://dnschecker.org/#A/alash-zan.kz

---

## 🔒 Установка SSL сертификата

### ⚠️ Важно: Выполнять ТОЛЬКО после обновления DNS!

DNS записи должны указывать на наш сервер, иначе Let's Encrypt не сможет подтвердить владение доменом.

### Автоматическая установка (рекомендуется)

1. **Загрузить скрипт на сервер:**
```bash
# С локального компьютера
scp -i docs/03-deployment/Claude-Alash-Zan-key.pem docs/03-deployment/setup-ssl.sh ubuntu@194.32.142.237:~/
```

2. **Подключиться к серверу:**
```bash
ssh -i docs/03-deployment/Claude-Alash-Zan-key.pem ubuntu@194.32.142.237
```

3. **Запустить скрипт:**
```bash
chmod +x ~/setup-ssl.sh
./setup-ssl.sh
```

Скрипт автоматически:
- ✅ Проверит DNS записи
- ✅ Создаст резервную копию конфигурации Nginx
- ✅ Обновит Nginx конфигурацию с доменом
- ✅ Получит SSL сертификат от Let's Encrypt
- ✅ Настроит автоматическое перенаправление HTTP → HTTPS
- ✅ Проверит автообновление сертификата

---

### Ручная установка

Если предпочитаешь ручную настройку:

#### 1. Проверить DNS
```bash
dig alash-zan.kz +short
# Должно показать: 194.32.142.237
```

#### 2. Обновить Nginx конфигурацию
```bash
sudo nano /etc/nginx/sites-available/alash-zan
```

Изменить `server_name`:
```nginx
server {
    listen 80;
    listen [::]:80;
    server_name alash-zan.kz www.alash-zan.kz;  # Изменить эту строку

    # ... остальная конфигурация остается без изменений
}
```

#### 3. Проверить и перезагрузить Nginx
```bash
sudo nginx -t
sudo systemctl reload nginx
```

#### 4. Получить SSL сертификат
```bash
sudo certbot --nginx -d alash-zan.kz -d www.alash-zan.kz
```

Certbot автоматически:
- Получит сертификат
- Обновит конфигурацию Nginx
- Настроит перенаправление HTTP → HTTPS
- Настроит автообновление

---

## ✅ Проверка после установки SSL

### 1. Проверка доступности сайта
```bash
# HTTP (должен перенаправить на HTTPS)
curl -I http://alash-zan.kz

# HTTPS
curl -I https://alash-zan.kz
```

### 2. Проверка сертификата
```bash
# С сервера
sudo certbot certificates

# С локального компьютера
openssl s_client -connect alash-zan.kz:443 -servername alash-zan.kz < /dev/null 2>/dev/null | openssl x509 -noout -dates
```

### 3. Онлайн проверка SSL
- https://www.ssllabs.com/ssltest/analyze.html?d=alash-zan.kz
- Ожидаемая оценка: A или A+

### 4. Проверка автообновления
```bash
# Проверить таймер systemd
sudo systemctl status certbot.timer

# Тест обновления (dry-run, не обновляет реально)
sudo certbot renew --dry-run
```

---

## 🔄 Автоматическое обновление SSL

Certbot автоматически настраивает systemd timer для обновления сертификатов.

**Проверить статус:**
```bash
sudo systemctl status certbot.timer
```

**Сертификаты обновляются автоматически:**
- Проверка: 2 раза в день
- Обновление: За 30 дней до истечения
- Срок действия: 90 дней

**Ручное обновление (если нужно):**
```bash
sudo certbot renew
```

---

## 🔧 Управление сертификатами

### Просмотр установленных сертификатов
```bash
sudo certbot certificates
```

### Обновление конкретного сертификата
```bash
sudo certbot renew --cert-name alash-zan.kz
```

### Удаление сертификата (если нужно переустановить)
```bash
sudo certbot delete --cert-name alash-zan.kz
```

### Расширение сертификата (добавить поддомены)
```bash
sudo certbot --nginx -d alash-zan.kz -d www.alash-zan.kz -d api.alash-zan.kz
```

---

## 🚨 Troubleshooting

### Проблема: DNS еще не обновился
**Симптомы:**
```
Failed to verify domain ownership
```

**Решение:**
- Подождать обновления DNS (до 24 часов)
- Проверить DNS: `dig alash-zan.kz +short`
- Проверить глобально: https://www.whatsmydns.net/#A/alash-zan.kz

### Проблема: Certbot не может подтвердить домен
**Симптомы:**
```
Timeout during connect (likely firewall problem)
```

**Решение:**
```bash
# Проверить firewall
sudo ufw status

# Убедиться что порты 80 и 443 открыты
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Проверить Nginx
sudo systemctl status nginx
sudo nginx -t
```

### Проблема: Сертификат не обновляется автоматически
**Решение:**
```bash
# Проверить таймер
sudo systemctl status certbot.timer

# Если не активен, включить:
sudo systemctl enable certbot.timer
sudo systemctl start certbot.timer

# Тест обновления
sudo certbot renew --dry-run
```

---

## 📊 Мониторинг SSL

### Настроить уведомления об истечении сертификата

Certbot отправляет email за 20, 10 и 1 день до истечения сертификата.

**Email указан при получении сертификата:**
```bash
--email admin@alash-zan.kz
```

**Дополнительные сервисы мониторинга:**
- https://uptimerobot.com/ - мониторинг доступности сайта
- https://www.ssllabs.com/ - мониторинг SSL рейтинга
- https://certificatemonitor.org/ - уведомления об истечении

---

## 📝 Чеклист настройки

- [x] DNS A запись настроена: alash-zan.kz → 194.32.142.237
- [x] DNS AAAA запись настроена (IPv6)
- [ ] DNS записи распространились (проверить `dig alash-zan.kz +short`)
- [ ] Nginx конфигурация обновлена с доменом
- [ ] SSL сертификат получен от Let's Encrypt
- [ ] HTTP перенаправляется на HTTPS
- [ ] Сайт доступен по https://alash-zan.kz
- [ ] Сертификат автоматически обновляется (проверить `sudo certbot renew --dry-run`)
- [ ] SSL оценка A/A+ на ssllabs.com

---

## 🔗 Полезные ссылки

- **Let's Encrypt**: https://letsencrypt.org/
- **Certbot документация**: https://certbot.eff.org/
- **SSL Labs тест**: https://www.ssllabs.com/ssltest/
- **DNS проверка**: https://www.whatsmydns.net/
- **Nginx SSL конфигурация**: https://ssl-config.mozilla.org/

---

**Последнее обновление**: 09 января 2025
