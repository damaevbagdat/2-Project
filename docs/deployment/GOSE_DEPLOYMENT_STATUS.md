# 🚀 Статус развертывания gose.kz

**Дата начала:** 18 ноября 2025
**Текущий статус:** ✅ Готово к развертыванию
**Текущий этап:** Готовы все скрипты и конфигурации

---

## 📋 План развертывания

### ✅ Этап 1: Подготовка проекта (Завершен)

**Что сделано:**
- ✅ Обновлена конфигурация проекта для домена gose.kz
  - `astro.config.mjs` - изменен site на https://gose.kz
  - `package.json` - обновлены name, description, repository, homepage
- ✅ Создана директория `docs/deployment/` в проекте 2-Project
- ✅ Создан скрипт деплоя `deploy-gose.sh` для установки релизов на сервер
  - Создает структуру директорий `/var/www/gose/`
  - Распаковывает билд в `/var/www/gose/releases/[VERSION]/`
  - Переключает symlink `/var/www/gose/current` атомарно (zero-downtime)
  - Автоматически удаляет старые релизы (оставляет последние 5)
- ✅ Создан скрипт отката `rollback-gose.sh`
  - Показывает список доступных релизов
  - Позволяет откатиться на любую предыдущую версию
  - Мгновенный откат через переключение symlink
- ✅ Создан GitHub Actions workflow `.github/workflows/deploy-gose.yml`
  - Автоматический деплой при push в main
  - Ручной деплой через workflow_dispatch
  - Версионирование по timestamp и git hash
- ✅ Создана документация Nginx конфигурации `NGINX_GOSE_CONFIG.md`
  - HTTP и HTTPS конфигурации
  - Security headers
  - Gzip сжатие
  - Кеширование статики
- ✅ Создана документация SSL настройки `SSL_GOSE_SETUP.md`
  - Инструкции по установке Certbot
  - Получение SSL сертификата через Let's Encrypt
  - Автообновление сертификатов
  - Troubleshooting

**Созданные файлы:**
1. `docs/deployment/deploy-gose.sh` - Скрипт деплоя на сервере
2. `docs/deployment/rollback-gose.sh` - Скрипт отката версий
3. `.github/workflows/deploy-gose.yml` - GitHub Actions workflow
4. `docs/deployment/NGINX_GOSE_CONFIG.md` - Документация Nginx
5. `docs/deployment/SSL_GOSE_SETUP.md` - Документация SSL

### ⏳ Этап 2: Развертывание на сервере (Требуется выполнение)

**Что нужно сделать:**
- ⏳ Настроить DNS (A запись gose.kz → 194.32.142.237)
- ⏳ Загрузить скрипты на сервер
- ⏳ Создать и активировать конфигурацию Nginx
- ⏳ Получить SSL сертификат через Let's Encrypt
- ⏳ Выполнить первый деплой
- ⏳ Протестировать работу сайта и откат версий

---

## 🏗 Архитектура развертывания

### Структура на сервере

```
/var/www/gose/
├── releases/                           # Все версии
│   ├── 2025-11-18_12-30-45_a1b2c3/    # По timestamp + git hash
│   ├── v1.0.0/                         # По git tag
│   └── v1.1.0/                         # Последняя версия
│
├── current -> releases/v1.1.0/         # Symlink на текущую версию
│
└── shared/                             # Общие файлы между релизами
    └── logs/                           # Логи
```

### Процесс деплоя

```
GitHub (push to main)
    ↓
GitHub Actions
    ↓
Build проекта (npm run build)
    ↓
Упаковка в gose-build.tar.gz
    ↓
SSH на сервер 194.32.142.237
    ↓
Деплой в /var/www/gose/releases/[VERSION]/
    ↓
Атомарное переключение symlink "current"
    ↓
Nginx отдает новую версию (zero-downtime)
```

---

## 🔧 Технические детали

### Сервер
- **IP адрес:** 194.32.142.237
- **Пользователь:** ubuntu
- **SSH ключ:** `Claude-Alash-Zan-key.pem` (используется тот же что и для alash-zan.kz)
- **ОС:** Ubuntu 24.04 LTS

### Домен
- **Домен:** gose.kz
- **DNS:** Требуется настройка A записи на 194.32.142.237

### Разделение с alash-zan.kz
- **alash-zan.kz:** `/var/www/alash-zan/`
- **gose.kz:** `/var/www/gose/` (новый)
- Полностью отдельные директории, конфигурации Nginx и SSL сертификаты
- Никакого пересечения файлов или настроек

---

## 📝 Следующие шаги

1. **Создать скрипт отката** (`rollback-gose.sh`)
   - Показывает список доступных релизов
   - Позволяет откатиться на предыдущую или конкретную версию
   - Мгновенный откат через переключение symlink

2. **Создать GitHub Actions workflow** (`.github/workflows/deploy-gose.yml`)
   - Автоматический деплой при push в main
   - Ручной деплой через workflow_dispatch
   - Создание версионированных релизов

3. **Загрузить скрипты на сервер**
   - Подключиться по SSH
   - Скопировать `deploy-gose.sh` в `/home/ubuntu/`
   - Скопировать `rollback-gose.sh` в `/home/ubuntu/`
   - Установить права выполнения

4. **Настроить Nginx**
   - Создать конфигурацию `/etc/nginx/sites-available/gose`
   - Активировать сайт
   - Проверить конфигурацию

5. **Настроить SSL**
   - Обновить DNS записи для gose.kz
   - Получить SSL сертификат через Let's Encrypt
   - Настроить автообновление

6. **Протестировать**
   - Выполнить первый деплой
   - Проверить доступность сайта
   - Протестировать откат
   - Проверить работу SSL

---

## 🔐 GitHub Secrets

Для работы деплоя потребуются следующие secrets в GitHub:

| Secret Name | Value | Описание |
|-------------|-------|----------|
| `SSH_PRIVATE_KEY` | `-----BEGIN OPENSSH PRIVATE KEY-----...` | Приватный ключ для SSH |
| `SERVER_HOST` | `194.32.142.237` | IP адрес сервера |
| `SERVER_USER` | `ubuntu` | Пользователь SSH |

> **Примечание:** Используются те же credentials что и для alash-zan.kz

---

## 📚 Связанные документы

- `docs/deployment/deploy-gose.sh` - Скрипт деплоя на сервере
- `docs/deployment/rollback-gose.sh` - Скрипт отката (создать)
- `.github/workflows/deploy-gose.yml` - GitHub Actions workflow (создать)
- `docs/deployment/NGINX_GOSE_CONFIG.md` - Конфигурация Nginx (создать)
- `docs/deployment/SSL_GOSE_SETUP.md` - Настройка SSL (создать)

---

## ⚠️ Важные заметки

1. **Не пересекаться с alash-zan.kz:**
   - Используем отдельную директорию `/var/www/gose/`
   - Отдельная конфигурация Nginx
   - Отдельный SSL сертификат
   - Общий только сервер и SSH доступ

2. **Система откатов:**
   - Все релизы хранятся в `/var/www/gose/releases/`
   - Можно мгновенно откатиться на любой предыдущий релиз
   - Автоматически сохраняются последние 5 релизов

3. **Zero-downtime deployment:**
   - Атомарное переключение symlink
   - Сайт не падает во время деплоя
   - Пользователи не замечают обновления

---

**Последнее обновление:** 18 ноября 2025, 00:35
**Обновил:** Claude Code AI
