# 🚀 Следующие шаги для запуска GOSE.KZ

**Дата создания:** 18 ноября 2025
**Статус:** Готово к развертыванию

---

## ✅ Что уже сделано

Вся подготовительная работа завершена:

1. ✅ Проект настроен для домена gose.kz
2. ✅ Созданы все скрипты деплоя и отката
3. ✅ Настроен GitHub Actions для автоматического деплоя
4. ✅ Подготовлены конфигурации Nginx и SSL
5. ✅ Документация полностью обновлена

---

## 📝 Что нужно сделать

### Шаг 1: Настроить DNS

**Действие:** Добавить A записи для домена gose.kz

```
Type: A
Name: @
Value: 194.32.142.237
TTL: 3600

Type: A
Name: www
Value: 194.32.142.237
TTL: 3600
```

**Проверка:**
```bash
nslookup gose.kz
nslookup www.gose.kz
```

⏱️ **Время ожидания:** До 24 часов для распространения DNS

---

### Шаг 2: Подключиться к серверу

```bash
ssh -i "C:\Users\damae\OneDrive\Документы\GitHub\my-first-project\Kamila-key.pem" ubuntu@194.32.142.237
```

или используйте SSH ключ из проекта:
```bash
ssh -i "docs/03-deployment/Claude-Alash-Zan-key.pem" ubuntu@194.32.142.237
```

---

### Шаг 3: Загрузить скрипты на сервер

С локальной машины выполните:

```powershell
# Перейти в директорию проекта
cd "C:\Users\damae\OneDrive\Документы\GitHub\2-Project"

# Загрузить скрипт деплоя
scp -i "..\my-first-project\Kamila-key.pem" docs/deployment/deploy-gose.sh ubuntu@194.32.142.237:~/

# Загрузить скрипт отката
scp -i "..\my-first-project\Kamila-key.pem" docs/deployment/rollback-gose.sh ubuntu@194.32.142.237:~/

# Установить права выполнения (выполнить на сервере)
ssh -i "..\my-first-project\Kamila-key.pem" ubuntu@194.32.142.237 "chmod +x ~/deploy-gose.sh ~/rollback-gose.sh"
```

**Проверка:**
```bash
# На сервере
ls -la ~/ | grep gose
```

Должны быть файлы:
- `deploy-gose.sh` (права 755)
- `rollback-gose.sh` (права 755)

---

### Шаг 4: Настроить Nginx

**На сервере:**

```bash
# Создать конфигурацию Nginx
sudo nano /etc/nginx/sites-available/gose
```

**Скопировать упрощенную HTTP конфигурацию** из [NGINX_GOSE_CONFIG.md](NGINX_GOSE_CONFIG.md) (раздел "До установки SSL сертификата")

```bash
# Активировать сайт
sudo ln -s /etc/nginx/sites-available/gose /etc/nginx/sites-enabled/

# Проверить конфигурацию
sudo nginx -t

# Перезапустить Nginx
sudo systemctl reload nginx
```

**Проверка:**
```bash
curl -I http://gose.kz
# Должен вернуть: HTTP/1.1 404 Not Found (это нормально, пока нет деплоя)
```

---

### Шаг 5: Настроить GitHub Secrets

Перейти в репозиторий GitHub → Settings → Secrets and variables → Actions

**Secrets уже должны быть настроены** (используются те же что для alash-zan.kz):

| Secret Name | Value | Статус |
|-------------|-------|--------|
| `SSH_PRIVATE_KEY` | Содержимое приватного ключа | ✅ Уже есть |
| `SERVER_HOST` | `194.32.142.237` | ✅ Уже есть |
| `SERVER_USER` | `ubuntu` | ✅ Уже есть |

**Проверка:** Перейти в Actions → Deploy GOSE.KZ to Production → Run workflow

---

### Шаг 6: Выполнить первый деплой

**Вариант 1: Автоматический деплой (Рекомендуется)**

```bash
# На локальной машине
cd "C:\Users\damae\OneDrive\Документы\GitHub\2-Project"
git add .
git commit -m "feat: initial deployment configuration for gose.kz"
git push origin main
```

GitHub Actions автоматически запустит деплой.

**Вариант 2: Ручной деплой**

1. Перейти в GitHub → Actions
2. Выбрать workflow "Deploy GOSE.KZ to Production"
3. Нажать "Run workflow"
4. Выбрать ветку "main"
5. Нажать "Run workflow"

**Проверка:**
- Следить за логами в GitHub Actions
- После завершения проверить: http://gose.kz

---

### Шаг 7: Установить SSL сертификат

**На сервере:**

```bash
# Установить Certbot (если еще не установлен)
sudo apt update
sudo apt install certbot python3-certbot-nginx -y

# Получить SSL сертификат
sudo certbot --nginx -d gose.kz -d www.gose.kz

# Следовать инструкциям:
# 1. Ввести email
# 2. Согласиться с условиями (A)
# 3. Отказаться от подписки (N)
# 4. Выбрать редирект на HTTPS (2)
```

**Проверка:**
```bash
# Проверить сертификат
sudo ls -la /etc/letsencrypt/live/gose.kz/

# Проверить HTTPS
curl -I https://gose.kz
```

**Полная инструкция:** См. [SSL_GOSE_SETUP.md](SSL_GOSE_SETUP.md)

---

### Шаг 8: Обновить Nginx для HTTPS

**На сервере:**

```bash
# Обновить конфигурацию
sudo nano /etc/nginx/sites-available/gose
```

**Заменить конфигурацию** на полную HTTPS версию из [NGINX_GOSE_CONFIG.md](NGINX_GOSE_CONFIG.md)

```bash
# Проверить конфигурацию
sudo nginx -t

# Перезапустить Nginx
sudo systemctl reload nginx
```

**Проверка:**
```bash
# HTTP должен редиректить на HTTPS
curl -I http://gose.kz

# HTTPS должен работать
curl -I https://gose.kz

# Проверить в браузере
```

---

### Шаг 9: Протестировать откат

**На сервере:**

```bash
# Посмотреть доступные версии
~/rollback-gose.sh

# Откатиться на предыдущую версию (если есть несколько)
~/rollback-gose.sh 2

# Проверить сайт
curl -I https://gose.kz
```

---

### Шаг 10: Финальная проверка

**Чек-лист:**

- [ ] DNS настроен и работает (gose.kz → 194.32.142.237)
- [ ] Сайт доступен по HTTPS (https://gose.kz)
- [ ] Редирект HTTP → HTTPS работает
- [ ] SSL сертификат валидный (зеленый замок)
- [ ] Автоматический деплой работает (GitHub Actions)
- [ ] Откат версий работает (rollback-gose.sh)
- [ ] Сайт полностью отделен от alash-zan.kz

**Проверка SSL качества:**
- Открыть: https://www.ssllabs.com/ssltest/
- Ввести: gose.kz
- Цель: Оценка A или A+

---

## 🔍 Полезные команды

### На сервере

```bash
# Проверить текущую версию
readlink /var/www/gose/current

# Список всех релизов
ls -lt /var/www/gose/releases/

# Просмотреть логи Nginx
sudo tail -f /var/log/nginx/gose-access.log
sudo tail -f /var/log/nginx/gose-error.log

# Проверить статус Nginx
sudo systemctl status nginx

# Проверить автообновление SSL
sudo certbot renew --dry-run

# Ручное обновление SSL
sudo certbot renew
sudo systemctl reload nginx
```

### На локальной машине

```bash
# Запустить деплой вручную
# GitHub → Actions → Deploy GOSE.KZ to Production → Run workflow

# Проверить сайт
curl -I https://gose.kz

# Проверить редирект
curl -I http://gose.kz
```

---

## 📚 Документация

Вся документация находится в `docs/deployment/`:

- [GOSE_DEPLOYMENT_STATUS.md](GOSE_DEPLOYMENT_STATUS.md) - Статус развертывания
- [deploy-gose.sh](deploy-gose.sh) - Скрипт деплоя
- [rollback-gose.sh](rollback-gose.sh) - Скрипт отката
- [NGINX_GOSE_CONFIG.md](NGINX_GOSE_CONFIG.md) - Конфигурация Nginx
- [SSL_GOSE_SETUP.md](SSL_GOSE_SETUP.md) - Настройка SSL
- [NEXT_STEPS.md](NEXT_STEPS.md) - Этот документ

---

## ⚠️ Важные заметки

1. **Не пересекается с alash-zan.kz:**
   - Отдельная директория: `/var/www/gose/`
   - Отдельная конфигурация Nginx
   - Отдельный SSL сертификат
   - Общий только сервер

2. **Система откатов:**
   - Все релизы сохраняются
   - Можно откатиться мгновенно
   - Автоматическое удаление старых (хранятся последние 5)

3. **Zero-downtime:**
   - Сайт не падает во время деплоя
   - Атомарное переключение версий
   - Пользователи не замечают обновления

---

## 🆘 Помощь

**Если что-то пошло не так:**

1. Проверьте логи:
   ```bash
   sudo tail -50 /var/log/nginx/gose-error.log
   ```

2. Проверьте конфигурацию:
   ```bash
   sudo nginx -t
   ```

3. Откатитесь на предыдущую версию:
   ```bash
   ~/rollback-gose.sh
   ```

4. Проверьте GitHub Actions логи

5. См. раздел "Устранение проблем" в [SSL_GOSE_SETUP.md](SSL_GOSE_SETUP.md)

---

**Последнее обновление:** 18 ноября 2025
**Автор:** Claude Code AI

**Готово к запуску! 🚀**
