---
import { redirect } from 'astro:middleware';

// Redirect the root-level about page to the default language version (Russian)
export async function get() {
  return redirect('/ru/about', 301);
}
---